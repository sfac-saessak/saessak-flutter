
import 'package:flutter/material.dart';

class FriendPostScreen extends StatelessWidget {
  const FriendPostScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('게시글'),
    );
  }
}
